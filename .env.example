# Copy this file to `.env` and set your real API key there.
# Do NOT commit your real `.env` to version control.

# For local development with Vite (exposed to client builds):
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# Alternatively, set a server-side env var named `API_KEY` if running server-side.
API_KEY=your_gemini_api_key_here

# Example usage:
# 1) Copy to `.env` and run `npm install` then `npm run dev`.
# 2) The app will read `VITE_GEMINI_API_KEY` when built by Vite, or fallback to `API_KEY` in Node.
